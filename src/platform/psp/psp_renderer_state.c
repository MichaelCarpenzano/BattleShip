#include "psp_renderer_state.h"

void pspRendererStateInit2DUIProfile(void)
{
    /*
     * Backend state bootstrap placeholder:
     * - configure GU defaults for 2D/UI (scissor, alpha blend, no depth writes)
     * - preserve N64 display-list emission and translate only at interpretation
     * - leave 3D microcode-equivalent setup for later parity passes
     */
}
